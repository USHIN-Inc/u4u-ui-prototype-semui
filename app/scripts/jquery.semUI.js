(function ($) {
  $.fn.semUI = function (options) {
    var settings = $.extend({
      // Set default options here
      // i.e. option: 'option'
    }, options);

    return this.each(function(){
      // Do stuff for each matching element here
    });
  };
} (jQuery));
